# 🤖 LawBot — AI Assistant for Legal Documents (StuPro)

**LawBot** — это умный WhatsApp-бот для автоматического сбора, анализа и обработки документов клиентов (для МВД/СтуПРО). Бот принимает фото документов, профессионально сканирует их, исправляет ориентацию, конвертирует в PDF и раскладывает по папкам в облаке.

---

## 🚀 Основные возможности

* **📱 Интеграция с WhatsApp:** Прием файлов и команд через Twilio API.
* **👁️ Google Cloud Vision (OCR):**
    * **Профессиональный поворот:** Точное определение угла текста (вплоть до градуса) и автоматический поворот.
    * **Smart Crop (Умная обрезка):** Детекция границ документа с "защитой от дурака" (не обрезает документ до размера штампа).
    * **Распознавание текста:** Извлечение текста (иврит/русский/английский) для анализа.
* **🧠 ИИ-анализ (Гибридный метод):**
    * Использует **OCR-текст** вместо картинки для отправки в OpenAI.
    * **Обход Privacy Filters:** Позволяет классифицировать паспорта и ID, не вызывая блокировку безопасности OpenAI.
    * Определение типа документа (30+ типов для СтуПРО) и извлечение имени.
* **🖼️ Обработка изображений:**
    * Конвертация любых фото в PDF.
    * Улучшение контраста и читаемости (Enhance).
    * Сохранение **оригиналов** (`_orig`) и **чистовиков** (PDF).
* **☁️ Облачное хранилище:**
    * **Production:** Yandex Disk.
    * **Test:** Dropbox (ветка `develop`).
* **🔄 CI/CD:** Автоматический деплой через GitHub Actions с безопасным управлением ключами.

--- 

## 🛠 Технический стек

* **Язык:** Python 3.11
* **Фреймворк:** FastAPI (Webhooks)
* **OCR & Vision:** Google Cloud Vision API
* **AI Analysis:** OpenAI API (GPT-4o / GPT-4o-mini)
* **Мессенджер:** Twilio WhatsApp API
* **База данных:** SQLite + SQLModel
* **Инфраструктура:** Docker & Docker Compose
* **Сервер:** Linux (CentOS/Ubuntu)

---

## 📂 Структура проекта

```text
LawBot/
├── main.py                 # Точка входа, вебхуки WhatsApp
├── database.py             # Модели БД (Client, Document)
├── google_credentials.json # Ключ Google (генерируется в CI/CD, в git игнорируется!)
├── services/
│   ├── doc_processor.py    # Логика: Google Vision (угол, кроп, OCR) + ImageMagick
│   ├── openai_client.py    # Клиент OpenAI (принимает текст или фото)
│   └── yandex_disk.py      # Работа с облаками (Yandex/Dropbox)
├── temp_files/             # Временная папка (авто-очистка)
├── docker-compose.yml      # Прод
├── docker-compose.test.yml # Тест
└── .github/workflows/      # CI/CD пайплайны

### Часть 2 (От Установки до конца)

```markdown
## ⚙️ Установка и настройка

### 1. Переменные окружения (.env)
Создайте файл `.env` (для продакшена) или `.env.test`:

```ini
# --- Основные настройки ---
SECRET_KEY=super_secret_key_change_me
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=ша256_хеш

# --- API Ключи ---
OPENAI_API_KEY=sk-proj-...
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...

# --- Google Vision (Путь внутри контейнера) ---
GOOGLE_APPLICATION_CREDENTIALS=/app/google_credentials.json

# --- Облако (Выбрать одно) ---
STORAGE_PROVIDER=yandex
YANDEX_DISK_TOKEN=y0_...

# Для Dropbox (Test)
# STORAGE_PROVIDER=dropbox
# DROPBOX_TOKEN=sl...