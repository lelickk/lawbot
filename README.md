# ⚖️ LawBot AI — Умный помощник для Адвокатского Бюро

Система автоматизации приема и обработки документов от клиентов через WhatsApp.
Бот принимает фото/PDF, распознает их с помощью ИИ, классифицирует, проверяет комплектность и загружает в облачный архив (Яндекс.Диск).

## 🚀 Функционал

* **Интеграция с WhatsApp:** Прием документов в привычном для клиента чате.
* **AI Vision (GPT-4o):** Распознавание типа документа (Паспорт, Справка, Анкета и др.) и ФИО владельца даже с рукописных фото и сканов.
* **Поддержка PDF:** Автоматическая конвертация PDF-файлов в изображения для анализа.
* **Облачный Архив:** Автоматическое создание папок клиентов на Яндекс.Диске и загрузка файлов.
* **Контроль комплектности:** Система знает обязательный список документов и подсказывает клиенту, чего не хватает.
* **База данных:** Ведение реестра всех поступивших файлов (SQLite).
* **Команда "Статус":** Генерация отчета о ходе сбора документов по запросу.

## 🛠 Технологический стек

* **Язык:** Python 3.11
* **Backend:** FastAPI + Uvicorn
* **AI/ML:** OpenAI API (GPT-4o Vision)
* **Мессенджер:** Twilio API for WhatsApp
* **Storage:** Yandex.Disk API (yadisk)
* **Database:** SQLModel (SQLite)
* **Utils:** PyMuPDF (работа с PDF), Requests

## ⚙️ Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/ВАШ_НИК/LawBot.git](https://github.com/ВАШ_НИК/LawBot.git)
    cd LawBot
    ```

2.  **Создайте виртуальное окружение:**
    ```bash
    python -m venv venv
    .\venv\Scripts\activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте переменные окружения:**
    Создайте файл `.env` и заполните его по шаблону:
    ```env
    OPENAI_API_KEY=sk-...
    TWILIO_ACCOUNT_SID=AC...
    TWILIO_AUTH_TOKEN=...
    YANDEX_TOKEN=...
    ```

5.  **Запустите сервер:**
    ```bash
    uvicorn main:app --reload
    ```

## 📱 Использование

1.  Клиент отправляет фото или PDF в WhatsApp боту.
2.  Бот отвечает: *"✅ Принято: Паспорт. Осталось сдать: Анкета"*.
3.  Бот присылает ссылку на сохраненный файл в облаке.
4.  Для проверки списка отправьте текстовое сообщение: **"Статус"**.

## 📞 Контакты разработчика

Проект разработан в рамках автоматизации бизнес-процессов юридической фирмы.

Deployment configured!