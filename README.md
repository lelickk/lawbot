# ⚖️ LawBot AI — Умный архивариус для Адвокатского Бюро

Система автоматизации приема и обработки документов от клиентов через WhatsApp.
Бот принимает фото/PDF, распознает их с помощью ИИ, классифицирует, проверяет комплектность и загружает в облачный архив (Яндекс.Диск), предоставляя публичные ссылки.

## 🚀 Функционал

* **Интеграция с WhatsApp:** Прием документов и команд через Twilio API.
* **Асинхронная обработка:** Мгновенный ответ серверу и фоновая обработка тяжелых файлов (защита от тайм-аутов).
* **AI Vision (GPT-4o):** Распознавание типа документа (Теудат Зеут, Паспорт, Справки и др.) и ФИО владельца.
* **Стандартизация Архива:**
    * Любой входящий файл (JPG/PDF) сохраняется в **PDF**.
    * Файлы именуются по стандарту: `YYYY-MM-DD_Тип_Документа.pdf`.
    * Оригиналы сохраняются отдельно.
* **Интеграция с Яндекс.Диском:**
    * Автоматическое создание папок клиентов.
    * Генерация **публичных ссылок** на загруженные файлы.
* **База Данных (SQLModel):** Ведение реестра клиентов и сданных документов.
* **Контроль комплектности:** Система знает обязательный список документов и подсказывает, чего не хватает.

## 🛠 Технологический стек

* **Язык:** Python 3.11 (Slim)
* **Web Framework:** FastAPI + Uvicorn + Gunicorn
* **AI/ML:** OpenAI API (GPT-4o Vision)
* **Cloud Storage:** Yandex.Disk REST API
* **Database:** SQLModel (SQLite)
* **Processing:**
    * `opencv-python-headless` (Улучшение изображений CLAHE)
    * `img2pdf` (Конвертация фото в PDF)
    * `pdf2image` (Растрирование PDF для ИИ)
* **Infrastructure:** Docker, Docker Compose (Timezone: Asia/Jerusalem)

## ⚙️ Установка и запуск

### 1. Клонирование
```bash
git clone [https://github.com/lelickk/LawBot.git](https://github.com/lelickk/LawBot.git)
cd LawBot

2. Настройка окружения
Создайте файл .env (в продакшене используется GitHub Secrets):

OPENAI_API_KEY=sk-...
YANDEX_DISK_TOKEN=y0_...
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...

3. Запуск (Docker)

docker-compose up -d --build
Контейнер автоматически настроен на часовой пояс Иерусалима и имеет увеличенный тайм-аут воркеров (300 сек).

📂 Структура проекта

/
├── .github/workflows/deploy.yml  # CI/CD Пайплайн
├── services/
│   ├── doc_processor.py  # Логика: PDF<->JPG, AI анализ, Именование
│   ├── yandex_disk.py    # Логика: Загрузка, Папки, Публикация ссылок
│   └── openai_client.py  # Клиент GPT-4o
├── temp_files/           # Временные файлы
├── database.py           # Модели БД (Client, Document)
├── main.py               # Вебхук, Фоновые задачи, Отправка сообщений
├── Dockerfile
└── docker-compose.yml

📱 Использование
Загрузка: Клиент отправляет фото или PDF.

Обработка: Бот отвечает: "⏳ Принято..." (статус "прочитано").

Результат: Через 30-60 сек приходит сообщение:

✅ Сохранено: Теудат_Зеут 
🔗 Ссылка: ... 
❌ Осталось сдать: Чек, Справка

Проверка: Клиент пишет "Статус" -> получает полный отчет.

📞 Контакты разработчика
Проект разработан для автоматизации бизнес-процессов юридической фирмы.