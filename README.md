# 🤖 LawBot — AI Assistant for Legal Documents (StuPro)

![status-badge](https://img.shields.io/badge/status-active-brightgreen) ![made-with-love](https://img.shields.io/badge/made%20with-%E2%9D%A4-red)

LawBot — умный WhatsApp-бот для автоматического сбора, анализа и обработки документов клиентов (для МВД / СтуПРО). Бот принимает фото документов, профессионально сканирует их, исправляет ориентацию, конвертирует в PDF и раскладывает по папкам в облаке.

---

Содержание
- [Ключевые возможности](#ключевые-возможности)
- [Технический стек](#технический-стек)
- [Структура проекта](#структура-проекта)
- [Установка и настройка](#установка-и-настройка)
  - [Переменные окружения (.env)](#переменные-окружения-env)
  - [Google Cloud ключ](#google-cloud-ключ)
  - [Запуск через Docker](#запуск-через-docker)
- [Логика работы (pipeline)](#логика-работы-pipeline)
- [CI/CD (GitHub Actions)](#cicd-github-actions)
- [Полезные команды](#полезные-команды)
---

## Ключевые возможности

- 📱 Интеграция с WhatsApp через Twilio: приём файлов и команд.
- 👁️ Google Cloud Vision (OCR):
  - Профессиональный поворот: определение угла текста и автоматический поворот (до градуса).
  - Smart Crop: детекция границ документа с «защитой от дурака» (не обрезает до размера штампа).
  - Распознавание текста: поддержка иврит/русский/английский.
- 🧠 ИИ-анализ (гибридный метод):
  - Для большинства задач используется OCR-текст (машиночитаемый) вместо картинки — быстрее, дешевле, безопаснее.
  - Обход privacy-фильтров: классификация типов документов (30+ типов для СтуПРО) и извлечение имени без срабатывания безопасных фильтров OpenAI.
  - Фоллбек: если OCR пустой — отправляем картинку.
- 🖼️ Обработка изображений:
  - Конвертация в PDF, улучшение контраста, сохранение оригиналов (`*_orig`) и финальных PDF.
- ☁️ Облачное хранилище:
  - Production: Yandex Disk.
  - Test: Dropbox (ветка `develop`).
- 🔄 CI/CD: автоматический деплой через GitHub Actions с безопасным управлением ключами.

---

## Технический стек

- Язык: Python 3.11  
- Web framework: FastAPI (вебхуки)  
- OCR & Vision: Google Cloud Vision API  
- AI analysis: OpenAI (GPT‑4o / GPT‑4o‑mini)  
- Мессенджер: Twilio WhatsApp API  
- База данных: SQLite + SQLModel  
- Инфраструктура: Docker & Docker Compose  
- Серверы: Linux (CentOS/Ubuntu)  
- Доп. инструменты: ImageMagick / Pillow для обработки изображений

---

## Структура проекта

Примерная структура (ключевые файлы/папки):

```
LawBot/
├── main.py                 # Точка входа, вебхуки WhatsApp
├── database.py             # Модели БД (Client, Document)
├── google_credentials.json # Ключ Google (в CI декодируется из секретов)
├── services/
│   ├── doc_processor.py    # Логика: Google Vision (угол, кроп, OCR) + ImageMagick
│   ├── openai_client.py    # Клиент OpenAI (принимает текст или фото)
│   └── yandex_disk.py      # Работа с облаками (Yandex/Dropbox)
├── temp_files/             # Временная папка (авто-очистка)
├── docker-compose.yml      # Продакшн
├── docker-compose.test.yml # Тест
└── .github/workflows/      # CI/CD пайплайны
```

---

## Установка и настройка

Рекомендуется запускать в Docker. Перед запуском подготовьте файл окружения и ключи.

### Переменные окружения (.env)

Создайте `.env` для продакшна и/или `.env.test` для теста. Пример:

```ini
# --- Основные ---
SECRET_KEY=super_secret_key_change_me
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=sha256_hash_here

# --- API ключи ---
OPENAI_API_KEY=sk-...
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...

# --- Google Vision (путь внутри контейнера) ---
GOOGLE_APPLICATION_CREDENTIALS=/app/google_credentials.json

# --- Облако (выберите один) ---
STORAGE_PROVIDER=yandex
YANDEX_DISK_TOKEN=y0_...

# Для теста (Dropbox)
# STORAGE_PROVIDER=dropbox
# DROPBOX_TOKEN=sl...
```

> Важное правило безопасности: не коммитьте `.env` в репозиторий.

### Google Cloud ключ

- Файл `google_credentials.json` не хранится в репозитории.
- Локально: положите файл в корень проекта (или укажите путь в переменной `GOOGLE_APPLICATION_CREDENTIALS`).
- В CI/CD: храните ключ в GitHub Secrets закодированным в base64 (например, `GOOGLE_CREDENTIALS_BASE64`). В workflow он декодируется и записывается в контейнер перед запуском.

### Запуск через Docker

Production (порт 8001):
```bash
docker-compose up -d --build
```

Test (порт 8002):
```bash
docker-compose -f docker-compose.test.yml --env-file .env.test up -d --build
```

---

## Логика работы (pipeline)

1. Пользователь отправляет фото документа в WhatsApp.
2. Файл попадает на endpoint FastAPI (webhook от Twilio).
3. Google Vision:
   - Анализирует изображение, возвращает угол наклона и OCR-текст.
4. Image processing:
   - Поворачиваем картинку согласно углу.
   - Smart Crop: обрезаем только если документ занимает допустимый процент кадра.
   - Улучшаем контраст/читаемость.
5. Конвертация в PDF, сохраняем оригинал и финальную версию.
6. AI-классификация:
   - Если есть OCR-текст — отправляем текст в OpenAI (быстро, экономно).
   - Если текста нет — отправляем изображение (фоллбек).
   - Ожидаем структированный JSON, например:
     {"doc_type": "Passport", "person_name": "Ivanov Ivan"}
7. Сохранение в облако: /Clients/{Phone}/{Name}/{Date}_{Type}.pdf
8. Ответ пользователю (статус обработки).

Smart Crop rules:
- Если текст занимает 20%–90% — делаем кроп.
- Если <20% (штамп) или >90% (уже scan/фото целой страницы) — не кропаем.

---

## CI/CD (GitHub Actions)

Основные шаги рабочего процесса deploy.yml:

- Определение ветки (main → production, develop → test).
- Подключение по SSH к серверу.
- Декодирование `GOOGLE_CREDENTIALS_BASE64` → запись `google_credentials.json`.
- Генерация `.env` на сервере из секретов.
- Пересборка и перезапуск Docker-контейнеров.

В GitHub Secrets необходимо хранить:
- OPENAI_API_KEY
- TWILIO_ACCOUNT_SID / TWILIO_AUTH_TOKEN
- GOOGLE_CREDENTIALS_BASE64
- STORAGE_PROVIDER + соответствующие токены
- SSH ключи для деплоя и др.

---

## Полезные команды

Посмотреть логи (в реальном времени):

```bash
# Production
docker logs -f --tail 100 lawbot_app

# Test
docker logs -f --tail 100 lawbot_app_test
```

Принудительная пересборка:

```bash
docker-compose up -d --build --force-recreate
```

Очистка временных файлов (локально):
```bash
rm -rf temp_files/* || true
```

---

## Вклад и процесс разработки

1. Fork the repo
2. Создать ветку: `git checkout -b feature/<короткое-описание>`
3. Сделать изменения и юнит-тесты
4. Commit: `git commit -m "feat: описание"`
5. Push и открыть Pull Request

Рекомендуется:
- Линт: `flake8` / `ruff`
- Форматирование: `black`
- Тесты: `pytest`
- CI: каждый PR должен проходить тесты и статический анализ

---