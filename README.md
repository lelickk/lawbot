# 🤖 LawBot — AI Assistant for Legal Documents (StuPro)

LawBot — это умный WhatsApp-бот для автоматического сбора, анализа и обработки документов клиентов (для МВД/СтуПРО). Бот принимает фото документов, исправляет их ориентацию, конвертирует в PDF и раскладывает по папкам в облаке.

## 🚀 Основные возможности

* **Интеграция с WhatsApp:** Прием файлов и команд через Twilio API.
* **ИИ-анализ (GPT-4o):**
    * Определение типа документа (30+ типов для СтуПРО).
    * Извлечение имени клиента (на латинице).
    * **Умный поворот:** Использование метода "Циферблат" (Clock Face) для исправления ориентации (даже если фото вверх ногами).
* **Обработка изображений:**
    * Конвертация любых фото в PDF.
    * Исправление EXIF-ориентации (Samsung/iPhone).
    * Сохранение **оригиналов** (`_orig`) и **чистовиков** (PDF).
* **Облачное хранилище:**
    * **Production:** Yandex Disk.
    * **Test:** Dropbox (ветка develop).
* **Админка:** Веб-интерфейс для управления клиентами и просмотра логов загрузки.
* **CI/CD:** Автоматический деплой через GitHub Actions.

---

## 🛠 Технический стек

* **Язык:** Python 3.11
* **Фреймворк:** FastAPI
* **База данных:** SQLite + SQLModel
* **AI:** OpenAI API (GPT-4o)
* **Мессенджер:** Twilio WhatsApp API
* **Инфраструктура:** Docker & Docker Compose
* **Сервер:** Linux (CentOS/Ubuntu)

---

## 📂 Структура проекта

```text
LawBot/
├── main.py                 # Точка входа, вебхуки WhatsApp, админка
├── database.py             # Модели БД (Client, Document)
├── services/
│   ├── doc_processor.py    # Логика обработки: ИИ, поворот, конвертация
│   ├── openai_client.py    # Клиент OpenAI с защитой от сбоев JSON
│   └── yandex_disk.py      # Работа с Яндекс.Диском (и Dropbox для тестов)
├── temp_files/             # Временная папка (очищается автоматически)
├── docker-compose.yml      # Конфиг для Production
├── docker-compose.test.yml # Конфиг для Test
└── .github/workflows/      # Скрипты CI/CD

⚙️ Установка и запуск
1. Переменные окружения (.env)
Создайте файл .env (для продакшена) или .env.test (для теста) со следующими ключами:
# --- Основные настройки ---
SECRET_KEY=super_secret_key_change_me
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=ша256_хеш_от_пароля

# --- API Ключи ---
OPENAI_API_KEY=sk-proj-...
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...

# --- Облако (Выбрать одно) ---
# Для Яндекс.Диска (Prod)
STORAGE_PROVIDER=yandex
YANDEX_DISK_TOKEN=y0_...

# Для Dropbox (Test)
# STORAGE_PROVIDER=dropbox
# DROPBOX_TOKEN=sl...

2. Запуск через Docker
Production (порт 8001):
docker-compose up -d --build

Test (порт 8002):
docker-compose -f docker-compose.test.yml --env-file .env.test up -d --build

🧠 Логика работы (StuPro)
Бот знает список обязательных документов для СтуПРО (REQUIRED_DOCS).

Пользователь отправляет фото в WhatsApp.

Бот скачивает файл, убирает EXIF-теги.

ИИ анализирует картинку:

Понимает, где "верх" у текста (на 12, 3, 6 или 9 часов).

Определяет тип (например, Marriage_Certificate).

Читает имя.

Python поворачивает картинку, конвертирует в PDF.

Загрузка: В папку клиента /Clients/{Phone}/{Name}/ загружаются два файла:

2026-01-30_DocType.pdf (Обработанный)

2026-01-30_DocType_orig.jpg (Исходник)

Ответ: Бот присылает ссылку на файл и обновленный список того, что еще осталось сдать.

🔄 CI/CD (GitHub Actions)
Процесс деплоя настроен в .github/workflows/deploy.yml:

Ветка main → Деплой в Production:

Использует секреты YANDEX_DISK_TOKEN.

Порт 8001.

Контейнер lawbot_app.

Ветка develop → Деплой в Test:

Использует секреты DROPBOX_TOKEN.

Порт 8002.

Контейнер lawbot_app_test.

🚑 Полезные команды
Посмотреть логи (Прод):
docker logs -f --tail 50 lawbot_app

Посмотреть логи (Тест):
docker logs -f --tail 50 lawbot_app_test

Перезапустить контейнер:
docker-compose restart app