# ⚖️ LawBot AI — Умный архивариус для Адвокатского Бюро

Система автоматизации приема документов через WhatsApp.
Бот принимает фото и PDF, конвертирует их, распознает тип и имя владельца через GPT-4o Vision, и аккуратно раскладывает по папкам в корпоративном Яндекс.Диске.

---

## 🚀 Основной функционал

* **WhatsApp Интеграция:** Работает через Twilio API. Принимает файлы и текст.
* **Умное распознавание (AI):** Использует **GPT-4o** для анализа изображения. Определяет тип документа (Теудат Зеут, Чек, Справка) и ФИО клиента.
* **Работа с PDF:** Автоматически конвертирует первую страницу многостраничного PDF в изображение для анализа нейросетью.
* **Улучшение качества:** Использует **OpenCV** для подготовки "плохих" фото перед отправкой в ИИ.
* **Структурированный Архив (Yandex.Disk):**
    * Автоматически создает папки клиентов: `/Clients/+97250.../Иван_Иванов/`.
    * Сохраняет оригиналы файлов отдельно в `_Originals_`.
    * Переименовывает файлы по стандарту: `YYYY-MM-DD_Тип_Документа.jpg`.
    * **Защита от дублей:** Если файл существует, создает копию с индексом (не перезаписывает).
* **DevOps Ready:** Полностью докеризированное приложение с настроенным CI/CD пайплайном.

## 🛠 Технологический стек

* **Язык:** Python 3.11 (Slim)
* **Сервер:** FastAPI + Uvicorn + Gunicorn
* **AI:** OpenAI API (GPT-4o Vision)
* **Обработка изображений:** OpenCV (cv2), pdf2image (Poppler)
* **Облако:** Yandex Disk REST API
* **Инфраструктура:** Docker, Docker Compose
* **CI/CD:** GitHub Actions (автоматический деплой на VPS при пуше в `main`)

---

## ⚙️ Установка и запуск (Локально)

### Предварительные требования
Для работы с PDF необходимо установить системную библиотеку `poppler`.
* *MacOS:* `brew install poppler`
* *Linux:* `sudo apt-get install poppler-utils`
* *Windows:* Скачать Poppler и добавить в PATH.

### 1. Клонирование и зависимости
```bash
git clone [https://github.com/ВАШ_НИК/LawBot.git](https://github.com/ВАШ_НИК/LawBot.git)
cd LawBot

python -m venv venv
source venv/bin/activate  # или .\venv\Scripts\activate для Windows
pip install -r requirements.txt

2. Настройка окружения (.env)
Создайте файл .env в корне проекта:

Фрагмент кода

OPENAI_API_KEY=sk-proj-...
YANDEX_DISK_TOKEN=y0_AgAAA...
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...

3. Запуск
Bash

uvicorn main:app --reload --port 8000
Для теста локально можно использовать ngrok для проброса вебхука Twilio.


🐳 Запуск через Docker (Продакшн)
Проект настроен на работу через docker-compose.

Bash

# Сборка и запуск в фоновом режиме
docker-compose up -d --build
Структура Docker
Используется образ python:3.11-slim.

Устанавливаются системные зависимости для OpenCV (libgl1, libglib2.0) и PDF (poppler-utils).

Пароли пробрасываются через файл .env (в продакшене создается через GitHub Secrets).

🤖 CI/CD Пайплайн (GitHub Actions)
В репозитории настроен файл .github/workflows/deploy.yml. При команде git push origin main:

GitHub Action подключается к серверу по SSH.

Создает актуальный файл .env из Secrets репозитория.

Скачивает обновления кода.

Пересобирает Docker-контейнер.

Перезапускает сервис без простоя.

Необходимые секреты в GitHub:

HOST, USERNAME, SSH_KEY — доступ к VPS.

YANDEX_TOKEN, OPENAI_KEY, TWILIO_SID, TWILIO_TOKEN — ключи API.

Отличная идея. Текущий README немного устарел, так как мы перешли на Docker, изменили библиотеки (убрали PyMuPDF в пользу pdf2image, добавили OpenCV) и внедрили CI/CD.

Вот обновленная версия, которая полностью соответствует тому коду, который мы сейчас написали и задеплоили. Скопируй её в README.md.

Markdown

# ⚖️ LawBot AI — Умный архивариус для Адвокатского Бюро

Система автоматизации приема документов через WhatsApp.
Бот принимает фото и PDF, конвертирует их, распознает тип и имя владельца через GPT-4o Vision, и аккуратно раскладывает по папкам в корпоративном Яндекс.Диске.

---

## 🚀 Основной функционал

* **WhatsApp Интеграция:** Работает через Twilio API. Принимает файлы и текст.
* **Умное распознавание (AI):** Использует **GPT-4o** для анализа изображения. Определяет тип документа (Теудат Зеут, Чек, Справка) и ФИО клиента.
* **Работа с PDF:** Автоматически конвертирует первую страницу многостраничного PDF в изображение для анализа нейросетью.
* **Улучшение качества:** Использует **OpenCV** для подготовки "плохих" фото перед отправкой в ИИ.
* **Структурированный Архив (Yandex.Disk):**
    * Автоматически создает папки клиентов: `/Clients/+97250.../Иван_Иванов/`.
    * Сохраняет оригиналы файлов отдельно в `_Originals_`.
    * Переименовывает файлы по стандарту: `YYYY-MM-DD_Тип_Документа.jpg`.
    * **Защита от дублей:** Если файл существует, создает копию с индексом (не перезаписывает).
* **DevOps Ready:** Полностью докеризированное приложение с настроенным CI/CD пайплайном.

## 🛠 Технологический стек

* **Язык:** Python 3.11 (Slim)
* **Сервер:** FastAPI + Uvicorn + Gunicorn
* **AI:** OpenAI API (GPT-4o Vision)
* **Обработка изображений:** OpenCV (cv2), pdf2image (Poppler)
* **Облако:** Yandex Disk REST API
* **Инфраструктура:** Docker, Docker Compose
* **CI/CD:** GitHub Actions (автоматический деплой на VPS при пуше в `main`)

---

## ⚙️ Установка и запуск (Локально)

### Предварительные требования
Для работы с PDF необходимо установить системную библиотеку `poppler`.
* *MacOS:* `brew install poppler`
* *Linux:* `sudo apt-get install poppler-utils`
* *Windows:* Скачать Poppler и добавить в PATH.

### 1. Клонирование и зависимости
```bash
git clone [https://github.com/ВАШ_НИК/LawBot.git](https://github.com/ВАШ_НИК/LawBot.git)
cd LawBot

python -m venv venv
source venv/bin/activate  # или .\venv\Scripts\activate для Windows
pip install -r requirements.txt
2. Настройка окружения (.env)
Создайте файл .env в корне проекта:

Фрагмент кода

OPENAI_API_KEY=sk-proj-...
YANDEX_DISK_TOKEN=y0_AgAAA...
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
3. Запуск
Bash

uvicorn main:app --reload --port 8000
Для теста локально можно использовать ngrok для проброса вебхука Twilio.

🐳 Запуск через Docker (Продакшн)
Проект настроен на работу через docker-compose.

Bash

# Сборка и запуск в фоновом режиме
docker-compose up -d --build
Структура Docker
Используется образ python:3.11-slim.

Устанавливаются системные зависимости для OpenCV (libgl1, libglib2.0) и PDF (poppler-utils).

Пароли пробрасываются через файл .env (в продакшене создается через GitHub Secrets).

🤖 CI/CD Пайплайн (GitHub Actions)
В репозитории настроен файл .github/workflows/deploy.yml. При команде git push origin main:

GitHub Action подключается к серверу по SSH.

Создает актуальный файл .env из Secrets репозитория.

Скачивает обновления кода.

Пересобирает Docker-контейнер.

Перезапускает сервис без простоя.

Необходимые секреты в GitHub:

HOST, USERNAME, SSH_KEY — доступ к VPS.

YANDEX_TOKEN, OPENAI_KEY, TWILIO_SID, TWILIO_TOKEN — ключи API.

📂 Структура проекта
Plaintext

/
├── .github/workflows/   # Скрипты CI/CD
├── services/
│   ├── doc_processor.py # Основная логика: PDF->JPG, OpenAI, Naming
│   ├── openai_client.py # Клиент GPT-4o
│   └── yandex_disk.py   # Клиент Яндекса (создание папок, загрузка)
├── temp_files/          # Временное хранилище (очищается после загрузки)
├── Dockerfile           # Инструкция сборки образа
├── docker-compose.yml   # Оркестрация контейнеров
├── main.py              # Точка входа (FastAPI Webhook)
└── requirements.txt     # Список библиотек
📞 Контакты
Разработано в целях автоматизации юридического документооборота.